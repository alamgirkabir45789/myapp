@using Microsoft.AspNetCore.Identity
@using myportfolio.Model.Entity.User;
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
@{ var user = await UserManager.GetUserAsync(User); }

<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button">
                <i class="fas fa-bars"></i>
            </a>
        </li>
    </ul>

    <!-- Right navbar links -->
    <ul class="navbar-nav ml-auto">
        <!-- Messages Dropdown Menu -->
        <li class="nav-item dropdown">
            <a class="nav-link" data-toggle="dropdown" href="#">
                <div class="img">
                    <img src="~/quadrion/img/02.png" alt="User img" class="img-size-32 img-circle mr-3" style="border:1px solid #eee" />
                </div>
            </a>
            <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
                <a asp-page="/Admin/User/Details" asp-route-id="@user.Id" class="dropdown-item">
                    <div class="media">
                        <img src="~/quadrion/img/02.png" alt="User img" class="img-size-32 mr-3 img-circle" style="border:1px solid #eee" />
                        <div class="media-body" >
                            <h3 class="dropdown-item-title" style="font-size:12px">
                                @user.Email
                                <span class="float-right text-sm text-success">
                                    <i class="fas fa-star"></i>
                                </span>
                            </h3>
                            <p class="text-sm text-muted">Logged in</p>
                        </div>
                    </div>
                </a>
            
              @*  <a asp-page="/Admin/User/Details" asp-route-id="@user.Id" class="dropdown-item">
                    <div class="media">
                        <img src="~/quadrion/img/profile.jpg" alt="User img" class="img-size-32 mr-3 img-circle" style="border:1px solid #eee" />
                        <div class="media-body" >
                            <h3 class="dropdown-item-title " style="font-size:12px">
                                Profile
                            </h3>
                        </div>
                    </div>
                </a>*@
                <a asp-page="/Admin/ChangePassword" class="dropdown-item">
                    <div class="media">
                        <img src="~/quadrion/img/lock.jpeg" alt="User img" class="img-size-32 mr-3 img-circle" style="border:1px solid #eee" />
                        <div class="media-body">
                            <h3 class="dropdown-item-title" style="font-size:12px">
                                Change Password
                            </h3>
                        </div>
                    </div>
                </a>
                <div class="dropdown-divider"></div>    
                <a asp-page="/Admin/Signout" id="signOut" class="dropdown-item dropdown-footer">Sign Out</a>             

            </div>
        </li>
    </ul>
</nav>
<!-- /.navbar -->
